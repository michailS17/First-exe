/*write a program that stores the ASCII value of lowercase and upper case alphabets 
and prints the alphabets as five pairs per line asa follow. Aa Bb Cc Dd Ee*/
/*declare a 2D array 5 by 6 and initialize its elements as follow with random value 
                -the 1st two elements of 1st row
                -the 1st three elements of 2nd row 
                -all elements of 3rd raw to zero
                -the last two elements of 4th row
                -the four middle elements of 5th row*/
#include<iostream>
#include<string>
using namespace std;
int main()
{

  return 0;
}
